# Railway Production Environment
# Ces variables sont automatiquement injectées par Railway

# Base de données PostgreSQL (automatique via Railway)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT Configuration (à configurer manuellement)
JWT_SECRET=your-super-secure-jwt-secret-change-in-production
JWT_REFRESH_SECRET=your-super-secure-refresh-secret-change-too
JWT_EXPIRES=1h
JWT_REFRESH_EXPIRES=7d

# Autres
NODE_ENV=production
BCRYPT_SALT_ROUNDS=12

# Note: Le PORT est automatiquement défini par Railway